#version 330 core

in vec3 Color;
in vec2 TexCoord;
out vec4 color;
uniform sampler2D texture1;
uniform sampler2D texture2;
uniform float mixer;

void main(){
    color = mix(texture(texture1, TexCoord), texture(texture2, TexCoord), mixer);
}
