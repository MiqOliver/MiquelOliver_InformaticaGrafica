#version 330 core

// Input vertex data, different for all executions of this shader.

in vec3 position;
in vec3 color;
in vec2 texCoord;

out vec3 Color;
out vec2 TexCoord;

uniform mat4 trans;

void main(){
    gl_Position = trans * vec4(position, 1.0);

	Color = color;
    TexCoord = texCoord;
}